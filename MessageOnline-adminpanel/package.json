{
  "name": "messageonline-adminpanel",
  "version": "1.0.0",
  "description": "Admin panel for MessageOnline",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && node server.js",
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && nodemon server.js"
  },
  "dependencies": {
    "sql.js": "^1.12.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "dotenv": "^16.4.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
